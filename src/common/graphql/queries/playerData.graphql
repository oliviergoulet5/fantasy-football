query PlayerData($assists:InputRange,$goalsScored:InputRange,$offset:Int,$limit:Int)
{
players(assists:$assists,goalsScored:$goalsScored,offset:$offset,limit:$limit){
firstName,
lastName,
goalsScored,
assists,
ictIndex,
minutes,
redCards,
yellowCards
}
}